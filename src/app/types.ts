export interface Asterix_en_Obelix{
    id: string;
    firstame: string;
    infix: string;
    lastname: string;
}

// ik heb de , verandert in een ; als het niet werkt ff terug veranderen

export interface LoginCredentials {
    email: string;
    password: string;
}